# macOS
.DS_Store

# Google Drive sync artifacts
.tmp.driveupload
.tmp.drivedownload

# Claude Code local-only settings
.claude/settings.local.json

# Personal/per-user data — never goes in the public repo (lives in Firebase later)
private/

# Editor/OS noise
*.swp
Thumbs.db

# Admin tooling — service-account keys are SECRETS, never commit
service-account.json
tools/service-account.json
node_modules/
tools/node_modules/
tools/package-lock.json
tools/content-backup-*.json
